Compiling the code on Jupyter Notebook: 

#importing 
import numpy as np
import matplotlib.pyplot as plt
from sklearn.cluster import KMeans
from sklearn.cluster import MiniBatchKMeans
import pickle
import imp
import helita
cd ~/helita/helita/sim
from helita import kmeans as km



#interpolation of the axis
interp()
>>> 

wavelength_distinctions(wvl)
>>> 

time_import(self, tm, inertia, t0)

fit(t0)

create_km_map():
>>> image of the k-means map and

create_k_means_maps(self, wvl)

create_spectral_map(self, i3, wvl, ax)
#prints spectral map 
